
// Verificar que los datos estén cargados
if (typeof edificios === 'undefined') {
    console.error('[ERROR] No hay datos de edificios disponibles. Verifica la carga desde API en script_capa.js');
    var edificios = []; 
}

